#include"screen.h"


/*
作者：xushu
作用：显示俄罗斯方块
*/

 int main(){
      initGame(8);  // 初始化屏幕
      turnOn(0,1);
      turnOn(0,2);
      turnOn(0,3);
      turnOn(1,1);
      clearScreen();

      turnOn(1,1);
      turnOn(1,2);
      turnOn(1,3);
      turnOn(2,1);
      clearScreen();

      turnOn(2,1);
      turnOn(2,2);
      turnOn(2,3);
      turnOn(3,1);





      return 0;

}
